#ifndef FACTORIAL_H
#define FACTORIAL_H

namespace PSE{
    /**
     * \brief calculate the factorial of an integer
     *
     */
    unsigned factorial(
            unsigned n          ///< input integer to perform factorial on
            );
}
#endif
